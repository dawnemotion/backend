const browser = "FireFox";

if(browser === "Chrome" ){
  console.log("지원되는 브라우저입니다.")
  return
  }else if(browser === "Safari"){
  console.log("지원되는 브라우저입니다.")
  return
  }else if(browser === "Opera"){
  console.log("지원되는 브라우저입니다.")
  return
  }else if(browser === "FireFox"){
  console.log("지원되는 브라우저입니다.")
  return
  }else if(browser === "Edge"){
  console.log("일부기능만 지원되는 브라우저입니다.")
  return
  }else{
  console.log("지원되지 않는 브라우저입니다.")
}
